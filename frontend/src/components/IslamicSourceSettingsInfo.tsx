import React from 'react';

export function IslamicSourceSettingsInfo() {
  return (
    <p style={{ fontSize: '12px', color: '#6b7280' }}>
      IslamicGPT answers Islamic questions only from approved sources. If no reliable source is found, it refuses to answer rather than guess.
    </p>
  );
}
