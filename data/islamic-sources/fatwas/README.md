# Islamic Source Folder

Store JSON records for this source type.

Requirements:
- Use IslamicSourceChunk-compatible metadata.
- Use `verified_by_admin=true` and `approved_for_answers=true` only after review.
- Do not store fabricated religious text.
- For testing placeholders, include: `FOR TESTING ONLY - NOT A REAL RELIGIOUS SOURCE`.
